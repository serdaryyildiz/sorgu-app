{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "serdaryy",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && npx electron ./dist/main.js",
    "dev": "cross-env NODE_ENV=development VITE_DEV_SERVER_URL=http://localhost:5173 electron ./dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2",
    "electron": "^31.0.0",
    "cross-env": "^7.0.3"
  }
}
